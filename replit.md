# Project Overview

A simple Node.js HTTP server.

## Tech Stack
- Node.js 22
- Built-in `http` module

## Structure
- `index.js` — Main server entry point, serves on port 5000

## Running
- `node index.js` starts the server on port 5000
